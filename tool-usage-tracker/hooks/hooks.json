{
  "description": "Append every Skill and Agent (Task) tool call to a local JSONL log.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Skill|Task|Agent",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/bin/log-tool-usage.py\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
